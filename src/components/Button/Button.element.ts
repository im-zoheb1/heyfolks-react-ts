import styled from 'styled-components';

export const Button = styled.button`
  outline: none;
  letter-spacing: 0.5px;
  font-weight: 600;
  cursor: pointer;
  padding: 1.2rem 3rem;
  font-size: 1.8rem;
  display: inline;
  border: none;
`;
